<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Технические работы</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background: linear-gradient(-45deg, #141517, #1e2024, #2a1b3d, #141517);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            color: #ffffff;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        /* Анимация перелива фона */
        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .container {
            text-align: center;
            padding: 2rem;
            max-width: 600px;
            backdrop-filter: blur(8px);
            background: rgba(255, 255, 255, 0.03);
            border-radius: 24px;
            border: 1px solid rgba(255, 255, 255, 0.05);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
            animation: fadeIn 1.2s ease-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Стильная шестеренка/иконка */
        .icon-wrapper {
            margin-bottom: 1.5rem;
            position: relative;
            display: inline-block;
        }

        .icon {
            font-size: 4rem;
            display: inline-block;
            animation: float 4s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-10px) rotate(10deg); }
        }

        h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            background: linear-gradient(45deg, #a855f7, #6366f1);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.5px;
        }

        p {
            font-size: 1.1rem;
            color: #9ca3af;
            line-height: 1.6;
            margin-bottom: 2rem;
        }

        /* Индикатор загрузки */
        .loader-bar {
            width: 100%;
            height: 6px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            overflow: hidden;
            position: relative;
        }

        .loader-progress {
            width: 40%;
            height: 100%;
            background: linear-gradient(90deg, #a855f7, #6366f1);
            border-radius: 10px;
            position: absolute;
            animation: loading 2.5s infinite ease-in-out;
        }

        @keyframes loading {
            0% { left: -40%; }
            50% { left: 100%; }
            100% { left: 100%; }
        }

        .footer {
            margin-top: 2rem;
            font-size: 0.85rem;
            color: #4b5563;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="icon-wrapper">
            <span class="icon">🛠️</span>
        </div>
        <h1>Технические работы</h1>
        <p>Мы обновляем сервис, чтобы сделать его быстрее и удобнее. Скоро вернемся в строй, не теряйте!</p>
        
        <div class="loader-bar">
            <div class="loader-progress"></div>
        </div>

        <div class="footer">
            Скоро всё починим • © 2026
        </div>
    </div>

</body>
</html>