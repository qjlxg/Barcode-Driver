<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>RouteX Cloud</title>
  <meta name="description" content="RouteX Cloud secure workspace access">
  <style>
    :root {
      --bg: #0f172a;
      --panel: #111827;
      --panel-2: #1f2937;
      --text: #e5e7eb;
      --muted: #94a3b8;
      --line: #334155;
      --accent: #38bdf8;
      --accent-2: #0284c7;
      --danger: #f87171;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      background:
        radial-gradient(circle at top, rgba(56,189,248,.12), transparent 28%),
        linear-gradient(180deg, #0b1220 0%, #0f172a 100%);
      color: var(--text);
      font: 16px/1.5 Arial, sans-serif;
      padding: 24px;
    }
    .shell {
      width: 100%;
      max-width: 420px;
      background: rgba(17,24,39,.92);
      border: 1px solid var(--line);
      border-radius: 18px;
      box-shadow: 0 20px 60px rgba(0,0,0,.35);
      overflow: hidden;
    }
    .head {
      padding: 28px 28px 18px;
      border-bottom: 1px solid var(--line);
    }
    .brand {
      display: flex;
      align-items: center;
      gap: 12px;
      font-weight: 700;
      font-size: 20px;
    }
    .logo {
      width: 36px;
      height: 36px;
      border-radius: 10px;
      background: linear-gradient(135deg, var(--accent), var(--accent-2));
      display: grid;
      place-items: center;
      color: white;
      font-weight: 700;
    }
    .subtitle {
      margin: 10px 0 0;
      color: var(--muted);
      font-size: 14px;
    }
    form {
      padding: 24px 28px 28px;
    }
    label {
      display: block;
      margin-bottom: 8px;
      font-size: 14px;
      color: var(--muted);
    }
    input {
      width: 100%;
      height: 48px;
      border-radius: 12px;
      border: 1px solid var(--line);
      background: var(--panel-2);
      color: var(--text);
      padding: 0 14px;
      margin-bottom: 16px;
      outline: none;
    }
    input:focus {
      border-color: var(--accent);
      box-shadow: 0 0 0 3px rgba(56,189,248,.15);
    }
    button {
      width: 100%;
      height: 48px;
      border: 0;
      border-radius: 12px;
      background: linear-gradient(135deg, var(--accent), var(--accent-2));
      color: white;
      font-weight: 700;
      cursor: pointer;
    }
    button:hover {
      filter: brightness(1.05);
    }
    .note {
      margin-top: 14px;
      font-size: 13px;
      color: var(--muted);
      text-align: center;
    }
    .error {
      display: none;
      margin-top: 14px;
      padding: 12px 14px;
      border-radius: 12px;
      background: rgba(248,113,113,.12);
      border: 1px solid rgba(248,113,113,.28);
      color: #fecaca;
      font-size: 14px;
    }
    .foot {
      padding: 0 28px 24px;
      color: var(--muted);
      font-size: 12px;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="shell">
    <div class="head">
      <div class="brand">
        <div class="logo">R</div>
        <div>RouteX Cloud</div>
      </div>
      <p class="subtitle">Secure workspace access portal</p>
    </div>

    <form id="loginForm">
      <label for="email">Work email</label>
      <input id="email" type="email" placeholder="name@routex-cloud.space">

      <label for="password">Password</label>
      <input id="password" type="password" placeholder="Enter your password">

      <button type="submit">Sign in</button>

      <div class="error" id="errorBox">
        Authentication is temporarily unavailable. Please contact your service administrator.
      </div>

      <div class="note">SSO and workspace login are available only from approved networks.</div>
    </form>

    <div class="foot">
      © 2026 RouteX Cloud. Secure access gateway.
    </div>
  </div>

  <script>
    document.getElementById('loginForm').addEventListener('submit', function(e) {
      e.preventDefault();
      document.getElementById('errorBox').style.display = 'block';
    });
  </script>
</body>
</html>
